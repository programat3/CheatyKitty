using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CardManager : MonoBehaviour
{
    /*
    public GameObject cardPrefab;// Prefab de la carta
    public Transform cardHolder;
    public Transform spawnPoint1;
    public Transform spawnPoint2;// Punto de generación de cartas
    public Transform spawnPoint3;// Punto de generación de cartas

    void Start()
    {
        SpawnCard(spawnPoint1);
        SpawnCard(spawnPoint2);// Crear la primera carta al inicio
        SpawnCard(spawnPoint3);
    }

    public void SpawnCard(Transform spawnPoint)
    {
        if (spawnPoint)
        {
            GameObject newCard = Instantiate(cardPrefab, spawnPoint.position, Quaternion.identity, cardHolder);
            newCard.GetComponent<CardDrag>().SetSpawner(this); // Pasamos la referencia del spawner
        }
        //CREATE LOOSE SCREEN IF NO MORE CARDS
        //Create Losing Card Sound and Mechanic
        //else
        //{
        //    GameObject newCard = Instantiate(cardPrefab, spawnPoint1.position, Quaternion.identity);
        //    newCard.GetComponent<CardDrag>().SetSpawner(this); // Pasamos la referencia del spawner
        //}
        
    }
    */
}